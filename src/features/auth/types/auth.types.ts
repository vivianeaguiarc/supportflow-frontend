/**
 * Tipos de auth da feature, reexportados da camada canônica de contratos
 * (`src/types/auth.ts`), que segue o backend como fonte da verdade.
 */
export type {
  AuthState,
  AuthTokens,
  AuthUser,
  LoginRequest,
  MeResponse,
  TokenPairResponse,
} from "@/types/auth";
