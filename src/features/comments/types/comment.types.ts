/**
 * Tipos de comentários da feature reexportados da camada canônica de contratos
 * (`src/types/comment.ts`), que segue o OpenAPI do backend como fonte da verdade.
 */
export type {
  CommentVisibility,
  CreateCommentRequest,
  TicketComment,
  TicketCommentWithAuthor,
  UserSummary,
} from "@/types/comment";
